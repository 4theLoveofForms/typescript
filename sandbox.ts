
let mixedArray: any[] = [1, "string", false];

// mixedArray = 'fish' // error - can't assign a string to an array of numbers

let scrotum: { name: string, strength: number, isVillain: boolean } 

scrotum = { name: "Scrotum", strength: 100, isVillain: false }